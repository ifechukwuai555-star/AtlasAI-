# AtlasAI

White-and-green AI question-and-answer website.

## Secure setup
Set `OPENAI_API_KEY` privately on the server/hosting platform. Never put the real key in browser code or GitHub.

## Run
npm install
npm start

The browser calls `/api/ask`; only the backend calls OpenAI.
